@model indiedevlog.web.Models.ProjectPlanListingModel

@{
    ViewData["Title"] = $"{Model.ProjectName}";
}

<h1>latest updates for @Model.ProjectName</h1>

@foreach (var post in @Model.PlanUpdates)
{
    @Html.Partial("_PlanUpdate", post)

    <br />
}